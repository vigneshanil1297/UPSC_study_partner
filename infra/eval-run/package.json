{
  "name": "cse-eval-run",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloud Run service hosting the long-running gemini-3.1-pro evaluation call (off Vercel's 60s cap).",
  "scripts": {
    "build": "esbuild server.ts --bundle --platform=node --format=esm --target=node20 --packages=external --outfile=dist/server.mjs",
    "start": "node dist/server.mjs"
  },
  "dependencies": {
    "@google/genai": "^2.8.0",
    "@supabase/supabase-js": "^2.108.2",
    "express": "^4.21.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
